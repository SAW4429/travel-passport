# country-recommendations.json 작성 가이드

`country-recommendations.json`에 국가를 추가하는 방법입니다. 이 파일만 고치면 되고, `index.html`은 건드릴 필요 없습니다.

## 국가 코드

키는 **ISO 3166-1 alpha-2** (대문자 2자리)를 사용합니다. 예: 몽골 = `MN`, 일본 = `JP`, 베트남 = `VN`.

- 주의: 우산·변환 어댑터·보조배터리처럼 **모든 나라 공통으로 필요한 물건**은 여기에 넣지 않습니다. 그건 사이드바의 "여행 준비물 체크" 슬롯(쿠팡 위젯 자리 1~4)이 담당합니다.
- 이 파일은 **그 나라이기 때문에** 특별히 필요한 상품 1개, 그 나라만의 잘 알려지지 않은 문화·예절 주의사항 글 1개만 다룹니다.

## 항목 형식

```json
{
  "MN": {
    "countryName": "몽골",
    "product": {
      "reason": "밤낮 기온차가 커서 저녁에 급격히 추워짐",
      "name": "차량용 무릎담요",
      "widgetHtml": "<!-- 쿠팡파트너스에서 발급받은 위젯 코드 -->"
    },
    "culture": {
      "title": "몽골 여행 전 알아두면 좋은 예절과 주의사항",
      "url": "https://내블로그.tistory.com/글주소"
    }
  }
}
```

- `product`와 `culture`는 **둘 다 필수가 아닙니다.** 한쪽만 채워도 되고, 나라 자체를 아예 키에서 빼도 됩니다(데이터 없는 나라는 화면에서 자동으로 섹션이 숨겨집니다).
- `product.reason`: 왜 이 나라에서 이 상품이 필요한지 한 줄 설명.
- `product.name`: 상품명 (이 값이 없으면 상품 블록 전체가 숨겨집니다).
- `product.widgetHtml`: 쿠팡파트너스 위젯 코드를 붙여넣는 자리. 비워두면(`""`) 아무것도 렌더링되지 않습니다 — 위젯 코드가 준비되기 전까지는 빈 문자열로 두세요.
- `culture.title` / `culture.url`: 두 값이 **모두** 있어야 링크가 표시됩니다. 하나라도 비어있으면 링크 자체가 생성되지 않습니다.

## 새 국가 추가 예시

```json
{
  "MN": { "...": "..." },
  "JP": {
    "countryName": "일본",
    "culture": {
      "title": "일본 온천 이용 시 문신 관련 주의사항",
      "url": "https://내블로그.tistory.com/다른글주소"
    }
  }
}
```
(위 JP 예시처럼 `product` 없이 `culture`만 넣어도 정상 작동합니다.)

## 실수해도 괜찮은 것들

- JSON 문법이 깨져도(오타, 쉼표 누락 등) 사이트 전체가 죽지 않습니다. 해당 데이터만 조용히 무시되고, 콘솔에 경고만 남습니다.
- 존재하지 않는 국가 코드를 사이드바에서 열어도 에러 없이 섹션이 그냥 숨겨집니다.
