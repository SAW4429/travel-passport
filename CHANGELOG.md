# Changelog

## v2.10.4 (2026-08-11) — 재작업 2회차
v2.10.3 재확인 결과: 한국 시군구 라벨과 드래그 정지 시간(2초→0.3~1.3초)은 개선 확인됨. 남은 문제 2가지를
이번에 처리.

**1. 라벨 중앙정렬 — 일반화(국가 예외처리 아님)**
v2.10.3의 "가장 큰 하위 폴리곤" 로직은 `pathBBoxFromD()`(WORLD_DATA 국가·개별 성/주 라벨)에만 적용돼
있었고, `BLOBS`(줌아웃 시 국가명 라벨 + `focusBlob()` 확대 목표 지점에 쓰이는, 원본 데이터에 미리 계산된
cx/cy/bw/bh)는 전혀 손대지 않았음 — 그래서 상세지도가 있는 26개국(미국·캐나다·러시아·인도네시아·필리핀·
일본·프랑스 등)은 여전히 문제가 남아있었음. 이번엔 국가별 예외처리가 아니라, `BLOBS` 파싱 직후 모든 블롭의
cx/cy/bw/bh를 `pathBBoxFromD(b.d)`(가장 넓은 하위 폴리곤 기준, 국가 목록 없음)로 일괄 재계산하도록 수정 —
라벨 위치와 `focusBlob()` 확대 목표점이 같은 값을 쓰므로 둘 다 한 번에 고쳐짐.

*검증 (반복 3회, 매번 동일 결과로 우연 아님을 확인)*
- 27개 블롭 전체 원본 대비 보정값 비교: 미국 bw 717.9→115.5(지도 폭 720에 근접했었음), 러시아
  720→305.3(정확히 지도 전체 폭이었음), 인도네시아 91.7→21.8, 프랑스 235.2→25.9, 말레이시아·캐나다·
  필리핀·일본도 유의미하게 보정됨. 독일·스위스·중국처럼 영토가 흩어져 있지 않은 나라는 거의 안 바뀜(정상)
- 보정된 중심점과 그 나라 자신의 실제 성/주 데이터 중 가장 가까운 곳까지 거리 확인: 미국→캔자스(대륙
  미국의 전통적 지리적 중심, 거리 5.5), 캐나다→매니토바(7.38), 러시아→이르쿠츠크(8.22), 인도네시아→
  서수마트라(1.35), 필리핀→아우로라·루손섬(0.5), 일본→이시카와현(1.57), 프랑스→앵드르(0.41) — 전부
  실제 본토 위의 지명과 가까움
- 미국·캐나다·멕시코 중심점을 비교해 남북 순서가 지리적으로 맞는지 확인(보정 전엔 미국이 멕시코보다도
  서쪽에 있는 것처럼 나왔음, 보정 후 정상)
- 새로고침 반복 후 동일 결과 재확인
- 사용자가 언급한 13개국(미국·캐나다·러시아·인도네시아·필리핀·일본·그리스·프랑스·노르웨이·칠레·피지·
  키리바시·뉴질랜드) 중 그리스·노르웨이·칠레·피지·키리바시·뉴질랜드는 WORLD_DATA 소속이라 v2.10.3에서
  이미 정상이었음을 재확인, 나머지 7개국(상세지도 보유국)은 이번 수정으로 해결

**2. 드래그 렉 — 관측 가능한 지표 위주로 구조 개선**
지난 회차와 동일하게, 이 세션 환경에서는 `requestAnimationFrame`이 발동하지 않아 실제 페인트/프레임
타이밍은 이번에도 관측 불가능함(재확인함, 디버그 오버레이도 같은 한계). 대신 JS 차원에서 구조적으로 더
가볍게 만드는 방향으로 접근:
- 라벨 우선순위 재계산(`recomputeLabelVisibility`, 정렬+사각형 충돌 검사, 후보 최대 ~1700개)을 드래그
  중에는 스로틀이 아니라 완전히 건너뛰도록 변경. 이미 떠 있는 라벨의 위치 이동(`positionVisibleLabels`)만
  매 프레임 수행하고, 어떤 라벨을 보여줄지는 드래그가 끝나는 순간 한 번만 정확히 재계산
  — 실측: 드래그 20틱 시뮬레이션 중 재계산 호출 0회(계측 확인), 프레임당 비용 0.220ms→0.097ms(56% 감소)
- 뷰포트 밖 상세지역 path 컬링은 라벨 재계산과 분리해서 드래그 중에도 매 프레임 실행 — 컬링 자체엔
  정렬·충돌 검사가 없어서 저렴하고, 이전처럼 재계산 스로틀에 묶여 있으면 화면에 새로 들어오는 지역이
  지연되어 나타나는 문제를 방지함(실측: 드래그 중에도 1226/1480(83%) 컬링 비율 그대로 유지, 팝인 지연 없음)
- 드래그 중엔 지역 경계선의 `shape-rendering`을 `geometricPrecision`에서 `optimizeSpeed`로 낮췄다가
  드래그가 끝나면 원래대로 되돌리는 CSS 클래스 토글 추가

**측정 가능/불가능 항목 명시**: JS 실행 시간(위 수치), 컬링 비율, DOM 요소 수(SVG 전체 3445개 중 상세지역
path는 1480개, 그 중 깊은 확대 시 254개만 실제 렌더링됨)는 이번 세션에서 직접 측정·비교함. 실제 화면
프레임률·페인트 시간·체감 부드러움은 이 환경에서 측정 불가 — 사용자 확인 필요.

## v2.10.3 (2026-08-11) — 긴급 재작업
v2.10.2에서 "고쳤다"고 보고했던 확대·축소·드래그 렉이 실제 배포 사이트에서는 그대로였음. 원인은 검증
방법 자체의 결함이었음 — 로컬 자동화 브라우저 탭의 `requestAnimationFrame`이 아예 발동하지 않는 상태에서
"코드가 의도대로 바뀌었는지"만 확인하고 "실제로 화면이 부드러워졌는지"는 확인하지 못한 채 완료로 보고했음.
이번엔 그 검증 공백부터 메우고, 실제로 재현 가능한 버그 3건을 새로 찾아 고침.

**검증 방법 (이번에 새로 한 것)**
- Claude Browser 도구의 탭과, 확장 프로그램으로 연결한 실제 Chrome 탭 양쪽 모두 `document.hidden === true`로
  고정되어 있어(백그라운드 탭 취급) `requestAnimationFrame`이 몇 초를 기다려도 단 한 번도 발동하지 않음을
  확인함 — 이 세션에서 쓸 수 있는 어떤 자동화 도구로도 실제 페인트/합성(compositing) 타이밍은 관측 불가능
  하다는 뜻. 대신: (1) 실제 `PointerEvent`를 이벤트 리스너 경로 그대로 60회 연속 디스패치하면서
  `PerformanceObserver`의 `longtask`(50ms 이상 메인스레드 점유) 감지 — 시군구 단위 확대 상태에서 결과 0건.
  (2) `applyTransform()`을 드래그처럼 반복 호출하며 `performance.now()`로 직접 측정 — 평균 0.06ms, 최악
  0.5ms. 두 측정 모두 "JS 연산 자체가 몇 초씩 걸린다"는 가설은 배제시킴 — 남은 유력한 원인은 관측할 수
  없었던 페인트/합성 비용(최대 1480개 상세지역 path + 최대 1720개 라벨이 전부 zoomLayer 하위에 있음)
- 배포용 디버그 오버레이(`?debugfps=1`)를 추가해서, 이 환경의 한계와 무관하게 사용자가 실제 기기의 실제
  브라우저에서 FPS/프레임 시간을 직접 확인할 수 있게 함(기본은 꺼짐, `?debugfps=0`으로 끌 수 있음)

**확인·수정한 버그 3건**
1. **Graticule(위경도 격자선) 확대 시 두꺼워짐**: `.graticule`만 다른 경계선들과 달리 `stroke-width`가
   고정값(.35)이라 확대 배율을 전혀 상쇄하지 않고 있었음(다른 path는 전부 `var(--rw, ...)` 사용). 깊게
   확대할수록 실제 화면에서 선이 그대로 두꺼워져 "화면을 가로지르는 두꺼운 회색 줄"로 보인 것 — 다른
   경계선과 동일하게 `var(--rw, .35)`를 쓰도록 수정
2. **라벨 위치 오배치(바다 위에 뜨는 문제)**: 자오선(경도 180도)을 가로지르는 나라(피지, 키리바시 등)는
   지도 데이터가 지도 양쪽 끝(x=0, x=720)에 걸쳐 그려져 있어서, bbox를 전체 좌표의 min/max로 구하면 지도
   폭 전체(720)를 감싸는 엉뚱한 bbox가 나와 라벨이 지도 정중앙(바다)에 떴음(실측 확인: 피지 bbox 폭이
   정확히 720). 경로를 하위 다각형(섬)별로 나눠 그중 면적이 가장 큰 것을 대표 위치로 쓰도록 수정 — 수정
   후 피지 bbox는 지도 오른쪽 끝 근처의 작은 값(2.83×1.9)으로 정상화됨
3. **국내(한국) 라벨 누락**: 버그는 아니고 우선순위 계산의 실제 체감 문제 — 한국 시군구는 개별 면적이
   작아서 같은 확대 배율에서 중국/러시아의 성/주(면적이 훨씬 큼)와 순수 면적으로 경쟁하면 거의 항상 밀림
   (실측: 한국을 중심에 놓고 확대해도 라벨 40개 중 4개만 한국 지역). 국내 지역에 면적 가중치(10배)를 줘서
   완전히 밀려나지 않게 함(국내 지역끼리는 여전히 크기순 경쟁·충돌 감지 적용) — 같은 조건에서 17/40으로 개선

**성능 개선 (관측은 못 했지만 근거 있게 적용)**
- 뷰포트 밖으로 완전히 벗어난 상세 지역 path를 `display:none` 처리(뷰포트 마진 포함, 팬 시 자동 갱신).
  실측: 강남 중심 확대(scale 40) 시 상세지역 path 1480개 중 1224개(83%)가 렌더 트리에서 제외됨 — 방문/
  예정 색상, 클릭 등 기존 동작은 컬링 여부와 무관하게 정상 작동함을 확인

기존 기능(클릭/검색/탐색/필터/색상/국가별 추천) 전부 회귀 테스트 통과.

## v2.10.2 (2026-08-11)
- 지도 라벨 글자 크기 버그 수정: 라벨 `<text>`의 font-size/stroke-width "px" 값이 SVG viewBox 확대 배율
  (`pxPerUnit` = 화면 폭 ÷ 720, 보통 1.5~3배)만큼 그대로 부풀려져서 렌더링되고 있었음(실측: 20px 지정 시
  실제 46px로 렌더링, pxPerUnit=1.78과 정확히 일치). 줌 배율 상쇄용 `scale(1/scale)`은 zoomLayer의 확대만
  상쇄할 뿐 viewBox 자체의 확대 배율은 건드리지 않아서 생긴 문제였음. font-size/stroke-width를 적용할 때
  pxPerUnit으로 미리 나눠서 상쇄하도록 수정. 데스크톱 넓은 화면·모바일 좁은 화면·세계지도 축소~시군구 단위
  최대 확대까지 실측으로 확인(13px 지정 시 실제 약 17px 안팎으로 일관, 이전에는 뷰포트 폭에 비례해 계속 커짐).
  덤으로 `pxPerUnit`이 초기 로드 타이밍에 따라 폴백값(1)에 고정될 수 있던 경로도 `load` 이벤트에서 한 번 더
  갱신하도록 보강
- 확대/축소 버벅임 수정 (원인 2가지 모두 확인 후 수정):
  1. 휠/트랙패드 확대·축소가 이벤트마다 즉시(동기) `applyTransform()`을 실행해서, 짧은 시간에 여러 번 발생하는
     휠 이벤트가 브라우저 프레임 속도를 앞지르고 있었음. 핀치줌·드래그가 이미 쓰던 `scheduleRender()`
     (requestAnimationFrame 배치)를 휠에도 재사용하도록 수정 — 상태 갱신과 실제 DOM 반영을 분리
     (`setZoomAt` + `scheduleRender`). 연속 20회 휠 이벤트 발생 시 동기 DOM 쓰기 0회, 프레임당 1회로 배치되는
     것을 계측으로 확인
  2. `--rw`/`--rws` CSS 커스텀 프로퍼티를 매 프레임 갱신하고 있었는데, 이 값이 최대 ~1700개 path의
     `stroke-width`에 쓰여서 갱신할 때마다 전체 스타일 재계산을 유발하고 있었음. 이전 적용값 대비 5% 이상
     차이 날 때만 갱신하도록 수정 — 연속적인 작은 줌 변화에서 실제 CSS 변수 갱신 횟수가 절반 가까이 줄어드는
     것을 확인, 드리프트도 5% 이내로 유지됨을 scale 90까지 확인

## v2.10.1 (2026-08-11)
- 지도 라벨 시스템 전면 재설계 (겹침/가독성 문제 수정). 이전 방식은 넓이 기준 통과 여부만 보고 고정 폰트 크기로
  전부 표시해서, 축소 상태에서 라벨이 대량으로 겹치고("대한민국본" 같은 텍스트 뭉개짐 포함) 작은 나라 라벨이
  이웃 나라를 침범하는 문제가 있었음. 새 방식:
  - 화면상 크기·국가/하위지역 티어(국가 항상 우선)·국가별 추천 데이터 보유 여부로 우선순위 점수 산정
  - 뷰포트당 최대 표시 개수 제한(세계지도 축소 26개, 확대 상태는 데스크톱 40개/모바일 20개) — 우선순위 낮은 것부터 제외
  - 라벨끼리 사각형 충돌 검사를 실제로 구현, 겹치면 우선순위 낮은 쪽을 숨김 — "대한민국본"은 실제로 대한민국·일본 라벨이
    겹쳐서 생긴 현상이었고(데이터 자체는 정상), 충돌 감지로 해결됨을 확인
  - 글자 크기는 도형의 화면상 크기에 비례하되 상/하한을 두고, 하한 밑으로는 억지로 줄이지 않고 아예 숨김
  - 우선순위·충돌 재계산은 스로틀(최소 90ms 간격)해서 빠른 연속 줌 조작에도 버벅이지 않게 하고, 이미 표시 중인
    라벨의 위치 갱신은 매 프레임 그대로 부드럽게 유지
  - 표시/숨김 전환에 CSS opacity 트랜지션(.15s)을 적용해 라벨이 와르르 나타나거나 사라지는 느낌을 줄임
  - 초기 로드·대륙 확대·국가 확대·축소 복귀·팬 이동·빠른 연속 줌 각각 실제로 재현해 테스트 완료

## v2.10.0 (2026-08-11)
- 지도에 국가/지역명 라벨 표시 추가. 기존 줌 시스템(`scale`/`#zoomLayer.zoomed-out`)을 그대로 재사용:
  - 축소 상태에서는 국가명만(WORLD_DATA 국가 + 상세지도 26개국·한국의 블롭), 확대하면 하위 지역명(도/주/시군구)으로 자동 전환
  - 도형 bbox 대비 텍스트 폭 추정치로 안 맞으면 라벨 생략(겹침/삐져나옴 방지), 뷰포트 밖 라벨은 계산에서 제외
  - 흰색 외곽선(halo)으로 미방문/방문예정/방문완료 색 전부와 5개 테마 위에서 읽히게 처리(색상별 개별 대비 계산 없이 견고하게 해결)
  - 라벨은 `pointer-events:none` — 클릭은 기존처럼 도형에서만 발생, 검색/탐색/색칠 로직 변경 없음
  - 성능: 라벨 후보는 매 프레임(기존 rAF 스로틀) 갱신되며 1720개 전체 기준 프레임당 약 0.8ms — 체감 버벅임 없음

## v2.9.3 (2026-08-11)
- 일본(JP) 문화 정보 링크 URL 수정 (tistory.com/31 → /32)

## v2.9.2 (2026-08-11)
- 일본 국가별 추천 데이터 추가 (country-recommendations.json)
- country-recommendations.json이 GitHub 웹 UI로 직접 수정되면서 JSON 문법이 깨져있던 것(별개의 JSON 객체 두 개가 이어붙은 형태)을 발견해 하나의 유효한 객체로 복구

## v2.9.1 (2026-08-09)
- 서비스워커 캐시 갱신 전략 수정: `CACHE_NAME`이 고정값(`travel-passport-shell-v1`)이라 매 배포마다 수동으로 안 올리면 이전 캐시가 절대 무효화되지 않던 문제 발견. `APP_VERSION`을 도입해 `index.html`의 `appVersion`과 함께 올리도록 하고, 캐시 이름에 버전을 포함시켜(`travel-passport-v` + 버전) 배포 때마다 이전 캐시가 자동으로 지워지게 함
- 새 버전이 감지되면(이미 열려있는 탭 포함) "새 버전이 있어요. 새로고침 하시겠어요?" 배너를 은근하게 노출, 확인 시에만 새로고침 — 강제 새로고침으로 입력 중이던 내용을 잃지 않게 함. 탭이 다시 보일 때(`visibilitychange`)마다 업데이트를 능동적으로 확인
- 기존 PWA 오프라인 캐싱 자체는 그대로 유지(캐시 전략 변경 없음, 이름 규칙만 변경)
- `verify-deploy.js`에 서비스워커 캐시 버전이 `appVersion`과 일치하는지 자동 검증하는 항목 추가
- 로컬에서 구버전 캐시를 인위적으로 만든 뒤 새 서비스워커로 교체해 실제로 갱신 배너가 뜨고, 이전 캐시가 삭제되고, 새로고침 시 최신 버전이 반영되는 것을 확인함

## v2.9.0 (2026-08-09)
- 보류 항목 재검토 — 알림/리마인더: 서버 없이 브라우저 Notification API만으로 가벼운 버전 구현. 계획해둔 여행지가 있을 때만 은근하게 알림 권한 배너 노출(강제 아님, 언제든 닫기 가능), 권한 허용 시 재방문할 때마다(최대 3일에 한 번) "계획해둔 여행지 N곳" 로컬 알림. 앱이 닫혀있는 동안 서버가 보내는 진짜 푸시는 불가능(백엔드 필요)이라 하지 않음
- 보류 항목 재검토 — 순위표/소셜 증명: 이번엔 보류 유지. 실제로 Firestore가 이미 연동돼 있어(사용자별 여행 기록 저장용) 검토했으나, (1) 보안 규칙은 Firebase 콘솔에서 사람이 직접 설정해야 해서 이 저장소 작업만으로는 확실히 동작하게 만들 수 없고, (2) 완전 익명 집계라도 클라이언트에서 계산하려면 다른 사용자들의 원본 문서를 브라우저로 가져와야 해서 그 과정에서 실제로는 각 사용자의 여행 기록이 다른 사용자 브라우저에 노출되는 프라이버시 문제가 있음 — 무리한 리스크로 판단해 보류

## v2.8.0 (2026-08-09)
- 전역 에러 모니터링 추가: `window`의 `error`/`unhandledrejection`을 잡아 GA 이벤트 `js_error`(message/source/line)로 전송. 유료 에러 모니터링 서비스 없이 기존 GA만 활용
- `DEPLOY_CHECKLIST.md` 추가: 지도·여권·통계·챌린지·PWA·내보내기/가져오기·공유·쿠팡 슬롯·국가별 추천·온보딩·관리자 도구·탐색·에러 모니터링 등 지금까지 만든 모든 기능의 배포 전 확인 목록
- `verify-deploy.js` 추가: 프레임워크 없는 node 스크립트로 JSON 유효성, index.html 핵심 DOM/함수 존재, 관리자 도구 비노출 규칙, 버전 일관성을 자동 점검 (`node verify-deploy.js`)

## v2.7.0 (2026-08-09)
- "여행 준비물 체크" 범용 쿠팡 슬롯을 계절(현재 월)·대륙별 로테이션 구조로 전환: `COUPANG_CONFIG.byContinent`/`bySeason`/`fallback` (전부 빈 배열 — 실제 상품 데이터 없이 구조만 마련, 사람이 나중에 채움). 국가별 추천(country-recommendations.json) 섹션과는 완전히 별개 시스템으로 유지
- 성능 최적화(대용량 지역 데이터 지연 로딩) 실적용은 보류: 1단계 점검에서 확인한 대로 SVG 경로 렌더링(DOM 생성)은 이미 `requestIdleCallback` 청크 분할로 처리 중이었으나, 남은 병목은 26개국 상세지도 JSON 자체를 페이지 로드 시 한꺼번에 `JSON.parse`하는 부분. 이걸 실제로 지연 로딩하려면 데이터를 국가별 파일로 분리하고, `ALL_REGIONS`/`BY_CODE`를 전제로 하는 검색·통계·업적·탐색 패널 등 코드 전반이 "일부만 로드된 상태"를 견디도록 구조 변경이 필요해 위험도가 높다고 판단, 이번엔 보류하고 구체적 이유만 기록함

## v2.6.0 (2026-08-09)
- 관리자 도구(admin-country-recs.html) 일괄 등록 기능 추가: 콤마/탭 구분 텍스트를 여러 줄 붙여넣으면 국가별로 파싱해 한 번에 JSON 생성 (단건 입력과 동일한 검증 + 국가 코드 존재 여부 검증 추가), 검색엔진 노출 차단 규칙은 그대로 유지
- 지도 상단에 "탐색" 버튼 추가: 대륙별 필터 + 완료율/이름 정렬로 국가 목록을 보고 바로 이동 가능 (기존 검색창은 그대로 유지, 별도 확장). GA 이벤트 추가: map_filter_used, map_sort_used
- 버그 수정: 검색 결과·탐색 목록 클릭 시 사이드바가 열리자마자 즉시 닫히던 기존 버그 발견 및 수정 (전역 "바깥 클릭 시 사이드바 닫기" 리스너로 클릭 이벤트가 버블링되던 문제, stopPropagation 추가)

## v2.5.0 (2026-08-09)
- 첫 방문자 온보딩 카드 추가: `localStorage`에 방문 기록(`onboardingSeen` 플래그, 저장된 방문 상태)이 전혀 없는 진짜 신규 방문자에게만 3단계 이내의 가벼운 카드 노출 (전체 화면 모달 아님, 언제든 닫기 가능, 한 번 닫으면 다시 안 뜸)
  - GA 이벤트 추가: onboarding_shown, onboarding_dismissed, onboarding_completed
- SEO: `<head>`에 WebApplication JSON-LD 구조화 데이터 추가 (기존 OG/Twitter 메타 태그와 동일한 name/description/url, 평점·리뷰 등 허위 정보 없음, JSON 문법 검증 완료)
- 접근성: 아이콘만 있던 버튼 중 aria-label이 빠져있던 사이드바 닫기 버튼, 체크리스트 항목 삭제 버튼에 aria-label 보강
- 성능/접근성 점검 결과 (수정 없이 보고만): 이미지 alt 속성 전수 점검 완료(문제 없음, 국기 이미지는 인접 텍스트와 중복 방지를 위해 의도적으로 alt=""), 5개 테마 전체의 텍스트 색상 대비 WCAG AA 기준 통과 확인(조정 불필요), 지도 렌더링은 이미 기본 지역은 즉시·상세 지역(줌 필요)은 requestIdleCallback으로 분할 렌더링 중임을 확인 — 남은 개선 여지는 페이지 로드 시 26개국 상세지도 JSON을 한꺼번에 파싱하는 부분으로, 필요한 국가만 지연 로딩하려면 데이터 구조 자체를 별도 파일로 분리해야 해서 이번 작업 범위를 벗어나 별도 작업으로 남겨둠

## v2.4.0 (2026-08-09)
- 국가 추천 데이터 입력 보조 도구 추가: `admin-country-recs.html` (관리자 전용, 메인 사이트와 완전히 분리된 별도 파일)
  - `index.html`의 실제 `WORLD_DATA`/`DETAIL_ARRAYS`(BLOB_NAMES) 데이터를 그대로 읽어와 국가 드롭다운 구성 (오타·잘못된 코드 방지, 새 코드 체계 없음)
  - 국가 선택 → 상품/문화 정보 입력 → `country-recommendations.json` 형식의 JSON 조각 생성 → 클립보드 복사
  - 검증: 국가 미선택 차단, 상품·문화 정보 둘 다 공란이면 차단, URL이 http/https로 시작하지 않으면 차단
  - 파일을 직접 수정하지 않음 — 사람이 복사해서 `country-recommendations.json`에 붙여넣는 방식
  - 이미 등록된 국가 목록을 하단에 표시 (중복 작업 방지), 선택한 국가가 이미 등록돼 있으면 경고 표시
  - `robots.txt`에 Disallow 추가, `<meta name="robots" content="noindex,nofollow">` 적용, sitemap.xml·내비게이션 어디에도 링크하지 않음 — 검색엔진/일반 방문자에게 노출되지 않음

## v2.3.0 (2026-08-09)
- 국가별 맞춤 추천 시스템 추가: 사이드바에 "이 나라를 여행한다면" 섹션 (기존 범용 "여행 준비물 체크"와는 별개, 시각적으로 구분됨)
  - 그 나라이기 때문에 특별히 필요한 상품 1개 + 잘 알려지지 않은 문화/주의사항 글 1개를 국가 코드(ISO 3166-1 alpha-2)로 매핑
  - 데이터는 `country-recommendations.json`으로 분리 (index.html 수정 없이 이 파일만 고치면 국가 추가 가능)
  - 몽골(MN) 샘플 1건만 포함, 나머지 국가는 데이터 없음 → 해당 국가는 섹션 자동 숨김
  - fetch 실패/JSON 깨짐/필드 누락에도 사이트 나머지 기능은 정상 동작 (콘솔 경고만, 에러 없음)
  - GA 이벤트 추가: country_product_view, country_product_click, country_culture_click
  - 작성 가이드: `country-recommendations-guide.md`

## v2.2.0 (2026-08-09)
- SEO 메타 태그(OG, Twitter Card, description) 추가
- robots.txt, sitemap.xml 추가
- PWA 지원 (manifest, 홈 화면에 추가 유도 배너, 서비스워커 오프라인 앱 셸 캐싱)
- 지도 공유 요약 카드에 사이트 주소 워터마크 추가 (기존 공유 카드 기능 보강)
- 쿠팡파트너스 연동 슬롯 준비 (사이드바 "여행 준비물 체크" 접이식 섹션 + 설정 객체)
- GA 이벤트 추가: region_click, region_complete, data_export, share_click, pwa_prompt_shown, pwa_prompt_accepted
- 기능별 on/off 스위치(FEATURE_FLAGS) 도입

참고: 데이터 내보내기/가져오기(JSON 백업/복원)와 지도 요약 카드 공유 기능은 이번 버전 이전부터
이미 구현되어 있던 기능으로 확인되어, 이번 작업에서는 새로 만들지 않고 공유 카드에 워터마크만 추가했습니다.
