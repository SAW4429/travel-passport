# 프로젝트 지침 — 나의 여행 여권

## 배포

- 원격 저장소: `https://github.com/SAW4429/travel-passport.git` (브랜치: `main`)
- 배포는 GitHub Pages를 통해 이루어지며, 실제 서비스 URL은 `https://saw4429.github.io/travel-passport/` 입니다.
- **커밋만 하고 push를 하지 않으면 사이트에 절대 반영되지 않습니다.** 작업을 "완료"로 보고하기 전에 반드시 `git push`까지 마치세요.
- GitHub 웹 UI로 파일을 수동 업로드하는 방식은 더 이상 사용하지 않습니다. 모든 변경은 이 로컬 저장소 → `git push` 경로로만 이루어집니다.

## 세션 시작 시 확인

- `git remote -v`로 원격이 `origin` = 위 저장소로 올바르게 연결되어 있는지 먼저 확인하세요.

## 작업 완료 순서

커밋 → push → (가능하면) `https://saw4429.github.io/travel-passport/`에 접속해 실제 반영을 확인 → 그 다음에 작업 완료로 보고하세요.
